# Mixed scalar and struct in same declaration

struct Point {
    x: int,
    y: int,
}

fn test_scalar_then_struct() {
    var x, p = 2, Point(3, 5);
}

fn test_struct_then_scalar() {
    var p, x = Point(3, 5), 2;
}
