# all
struct Point { x: int, y: int }
property int_prop: int;

var sum = 0;
var p = Point(1, 1);
var i = 0;
loop {
    if i >= 5 {
        break;
    }
    sum = sum + p.x + p.y;
    p.x = p.x + 1;
    p.y = p.y + 1;
    i = i + 1;
}
int_prop = sum;
